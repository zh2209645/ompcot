{
  "name": "ompcot",
  "version": "0.7.5",
  "description": "Local Codex-style desktop GUI for the OMP coding agent",
  "keywords": [
    "ompcot",
    "omp",
    "coding-agent",
    "desktop",
    "multi-agent",
    "tauri"
  ],
  "author": "ZephyrQ",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zephyrq-z/ompcot.git"
  },
  "scripts": {
    "tauri": "tauri",
    "dev": "PATH=\"$HOME/.cargo/bin:$PATH\" tauri dev",
    "fetch:omp": "echo 'omp is found on PATH at runtime; no need to bundle'",
    "build:extensions": "bun run scripts/build-extensions.js",
    "build": "PATH=\"$HOME/.cargo/bin:$PATH\" tauri build",
    "prebuild": "bun run build:extensions",
    "release": "bash scripts/release.sh",
    "release:mac:dmg": "bash scripts/release-macos-dmg.sh",
    "test": "vitest run && bun run scripts/check-tauri-permissions.js",
    "test:watch": "vitest",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write . --unsafe",
    "format": "biome format .",
    "format:fix": "biome format --write .",
    "check": "biome check .",
    "check:fix": "biome check --write . --unsafe --formatter-enabled=false --linter-enabled=false",
    "check:permissions": "bun run scripts/check-tauri-permissions.js",
    "check:rust": "bash scripts/check-rust.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-fs": "^2.5.1",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "qrcode": "^1.5.0",
    "remend": "^1.3.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@tauri-apps/cli": "^2.11.2",
    "esbuild": "^0.28.0",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
